import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { DropPad } from '../DropPad';

<Title
  title="DropPad"
  subtitle="Use DropPad to manage file uploads."
/>

<SectionTitle>Simple</SectionTitle>

Handle file uploads using this component. This is an all in one solution to handling file uploads. So keep in mind that an `uploadUrl` is required in order to execute the query.

<Preview>
  <Story id="components-droppad--simple"/>
</Preview>

<SectionTitle>DropPad Props</SectionTitle>
<Props of={ DropPad } />
