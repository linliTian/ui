---
menu: Components
name: Accordion
---

import { Playground, Props } from 'docz'
import { Input } from '../../input/Input';
import { Button } from '../../button/Button';
import { Accordion } from '../Accordion';
import { AccordionItem } from '../AccordionItem';
import { StyledCollapseContent, CollapseContent, AccordionContainer, ThemeContainer } from '../../../../docz/components'

# Accordion
### Accordions allow users to display content in an organized, space efficient way.

## Standard

The standard accordion will allow users to expand multiple sections. Set the `classic` prop to `false` to achieve this behaviour.

<Playground>
  <ThemeContainer>
    <AccordionContainer>
      <Accordion classic={false} defaultExpandedItems={['1', '3']} itemGap={20}>
        <AccordionItem itemKey="1" footer={<Button>Submit</Button>}>
          <CollapseContent>
            <Input label={'First Name'} />
            <Input label={'Last Name'} />
            <Input label={'Email'} />
            <Input label={'Phone Number'} />
          </CollapseContent>
        </AccordionItem>
        <AccordionItem itemKey="2">
          <CollapseContent />
        </AccordionItem>
        <AccordionItem itemKey="3">
          <CollapseContent />
        </AccordionItem>
      </Accordion>
    </AccordionContainer>
  </ThemeContainer>
</Playground>

## Classic

The standard accordion will allow users to expand a single section at a time. Set the `classic` prop to `true` to achieve this behaviour.

<Playground>
  <ThemeContainer>
    <AccordionContainer>
      <Accordion itemGap={20} classic>
        <AccordionItem itemKey="1">
          <CollapseContent />
        </AccordionItem>
        <AccordionItem itemKey="2">
          <CollapseContent />
        </AccordionItem>
        <AccordionItem itemKey="3">
          <CollapseContent />
        </AccordionItem>
      </Accordion>
    </AccordionContainer>
  </ThemeContainer>
</Playground>

## External Accordion Control

Take external control of the accordion by passing `expandedItems` that are managed outside of the `Accordion`.

<Playground>
  {() => {
    const itemKeys = ['1', '2', '3']
    const [selectedItems, setSelectedItems] = React.useState([])
    const handleOnChange = (key) => {
      if(selectedItems.includes(key)) {
        setSelectedItems(selectedItems.filter(i => i !== key));
      } else {
        setSelectedItems(selectedItems.concat(key));
      }
    }
    
    const handleOnClick = () => {
      if(selectedItems.length === itemKeys.length){
        setSelectedItems([])
      } else {
        setSelectedItems(itemKeys)
      }     
    }
    const isFullyExpanded = selectedItems.length === itemKeys.length
    return (
      <ThemeContainer>
        <AccordionContainer>
          <Button onClick={handleOnClick}>
            {isFullyExpanded ? 'Close All' : 'Expand All' }
          </Button>
          <div style={{ height: '20px' }} />
          <Accordion
            itemGap={20}
            expandedItems={selectedItems}
            defaultExpandedItems={selectedItems}
            onChange={handleOnChange}
          >
            <AccordionItem itemKey={itemKeys[0]} footer={<div>A footer</div>}>
              <CollapseContent />
            </AccordionItem>
            <AccordionItem itemKey={itemKeys[1]} disabled>
              <CollapseContent />
            </AccordionItem>
            <AccordionItem itemKey={itemKeys[2]}>
              <CollapseContent />
            </AccordionItem>
          </Accordion>
        </AccordionContainer>
      </ThemeContainer>
    )
  }
}
</Playground>

## Accordion Props
<Props table={true} of={Accordion} />

## AccordionItem Props
<Props table={true} of={AccordionItem} exclude={['expanded', 'onChange']} />

## Theme

To modify the accordion item theme, please see the `Collapse` component.
